{
  "name": "cron-next",
  "version": "0.1.0",
  "private": true,
  "description": "Prints the next run times for a 5-field cron expression.",
  "type": "module",
  "bin": {
    "cron-next": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
