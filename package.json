{
  "name": "cron-next",
  "version": "0.1.0",
  "description": "Prints the next run times for a 5-field cron expression.",
  "type": "module",
  "license": "MIT",
  "author": "kjjackson8",
  "keywords": ["cron", "cli", "schedule", "crontab"],
  "files": [
    "dist"
  ],
  "bin": {
    "cron-next": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "node --test dist/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
